import { veSignImages } from "@/constants/images";
import { ProjectData } from "../types";

const images = veSignImages.slice(0, 4)

export const veSign: ProjectData = {
  name: "veSign",
  slug: "veSign",
  year: "2022",
  images,
};